/**
 * ============================================================
 * Custom Hooks — Barrel Export
 * ============================================================
 *
 * Re-exports all custom React hooks from a single entry point.
 * Import from "@/hooks" instead of individual files.
 *
 * Hooks to add here later:
 *  - useMediaQuery     (responsive breakpoint detection)
 *  - useLocalStorage   (typed localStorage read/write)
 *  - useDebounce       (debounced search input)
 *  - useSupabaseAuth   (authentication state helper)
 *  - useScrollLock     (lock body scroll when drawer/modal is open)
 *
 * Example usage:
 *   import { useMediaQuery, useDebounce } from "@/hooks";
 * ============================================================
 */

// TODO: Export custom hooks as they are created
// export { useMediaQuery } from "./use-media-query";
// export { useLocalStorage } from "./use-local-storage";
// export { useDebounce } from "./use-debounce";
