/**
 * ============================================================
 * Layout Components — Barrel Export
 * ============================================================
 *
 * Re-exports all app-shell layout components from a single entry point.
 * Import from "@/components/layout" instead of individual files.
 *
 * Components to add here later:
 *  - Header        (top navigation bar with logo, search, cart icon)
 *  - BottomNav     (mobile bottom tab bar — Home, Categories, Cart, Profile)
 *  - Footer        (minimal footer for desktop / about pages)
 *  - MobileMenu    (slide-out hamburger menu)
 *
 * Example usage:
 *   import { Header, BottomNav } from "@/components/layout";
 * ============================================================
 */

// TODO: Export layout components as they are created
// export { Header } from "./header";
// export { BottomNav } from "./bottom-nav";
// export { Footer } from "./footer";
