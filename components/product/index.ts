/**
 * ============================================================
 * Product Components — Barrel Export
 * ============================================================
 *
 * Re-exports all product-specific UI components from a single entry point.
 * Import from "@/components/product" instead of individual files.
 *
 * Components to add here later:
 *  - ProductCard       (thumbnail card for grid/feed views)
 *  - ProductDrawer     (swipe-up bottom sheet with full product details)
 *  - ProductGallery    (Embla-powered swipeable image carousel)
 *  - ProductVideoFeed  (TikTok-style vertical video scroll)
 *  - QuickAddButton    (one-tap "Add to Cart" action button)
 *
 * Example usage:
 *   import { ProductCard, ProductDrawer } from "@/components/product";
 * ============================================================
 */

// TODO: Export product components as they are created
// export { ProductCard } from "./product-card";
// export { ProductDrawer } from "./product-drawer";
// export { ProductGallery } from "./product-gallery";
