/**
 * ============================================================
 * Common Components — Barrel Export
 * ============================================================
 *
 * Re-exports all small, shared widgets from a single entry point.
 * Import from "@/components/common" instead of individual files.
 *
 * Components to add here later:
 *  - Loader        (spinner / skeleton loading indicator)
 *  - Badge         (price tags, discount pills, "New" labels)
 *  - PriceTag      (formatted price display with ₹ symbol)
 *  - EmptyState    (friendly "nothing here" placeholder)
 *  - ThemeToggle   (button to cycle Light → Dark → Luxury themes)
 *
 * Example usage:
 *   import { Loader, Badge } from "@/components/common";
 * ============================================================
 */

// TODO: Export common components as they are created
// export { Loader } from "./loader";
// export { Badge } from "./badge";
// export { PriceTag } from "./price-tag";
// export { ThemeToggle } from "./theme-toggle";
